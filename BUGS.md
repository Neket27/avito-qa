# 🧪 Тестирование API Avito (Internship)

Тестирование эндпоинтов:
- `POST /item` — создание объявления  
- `GET /item/{id}` — получение объявления  
- `GET /statistic/{id}` — получение статистики  

✅ **Позитивные сценарии** работают.  
⚠️ **Обнаружены критические и средние баги**.

---

## 🚨 Критические баги

| ID | Описание | Пример запроса | Ответ |
|----|----------|----------------|-------|
| **BUG-001** | Отрицательные значения (`-1`) принимаются во всех числовых полях | ```json { "price": -1, "statistics": { "likes": -1, "viewCount": -1, "contacts": -1 }, "sellerID": -1 } ``` | ✅ `200 OK`, `"status": "Сохранили объявление - ..."` |
| **BUG-002** | `GET /item/{id}` возвращает **массив** из 1 объекта, а не объект | `GET /item/e3aa8d35-c2d9-42c3-9a50-b4a45d8b47cf` | ✅ `200 OK`, тело: `[ { ... } ]` |
| **BUG-003** | `GET /statistic/{id}` возвращает **массив**, а не объект | `GET /statistic/{id}` | ✅ `200 OK`, тело: `[ { "contacts":1, "likes":1, "viewCount":1 } ]` |

>  **Риск**: клиенты не смогут десериализовать ответ в `Statistic` / `Item` - падение интеграций.

---

## ⚠️ Баги средней важности

| ID | Описание | Факт из логов |
|----|----------|---------------|
| **BUG-004** | `0` в числовых полях → сообщение `"поле X обязательно"` (вводит в заблуждение: поле есть, но значение недопустимо) | `price: 0` → `"поле price обязательно"`<br>`likes: 0` → `"поле likes обязательно"`<br>`sellerID: 0` → `"поле sellerID обязательно"` |
| **BUG-005** | Спецсимволы и 1-символьные значения в `name` принимаются без валидации | `name: "И"`, `"/"`, `"\"`, `"?"`, `"+"`, `"-"`, `"#"`, `"@"`, `"&"`, `"^"`, `"%"`, `"="`, `"("`, `")"`, `"'"` → ✅ `200 OK` |
| **BUG-006** | Переполнение (`> Long.MAX_VALUE`) → неинформативная ошибка | `price: 9223372036854775808` → `status: "не передано тело объявления"`, `message: ""` |

---
